<!-- Translated (zh-CN) from docs/how-to/build-custom-dashboards.md · source-sha256:6b2f70f7a08b3bae · hand-translated by Claude, not generated by scripts/translate-docs.py - edit the source file and retranslate if it changes, don't machine-retranslate over this. -->

# 如何构建自定义仪表板

将您已经在 Logs、Traces 和 Metrics 页面上构建好的查询组合成一个**多面板仪表板**
——一个保存的对象包含多个面板,而不是需要逐个打开的多个独立的已保存搜索。关于这一
功能背后的存储与执行设计,参见[该 ADR](../../docs-internal/adr/0023-custom-dashboards.md);
关于单页面的等效功能(一个已保存的过滤器,没有面板),参见下方的
[已保存搜索与仪表板](#已保存搜索与仪表板)。

## 前提条件

- 一个可以在浏览器中访问的正在运行的 Flare 实例(可以是[独立部署](run-standalone.zh-CN.md)、
  [Aspire](run-with-aspire.zh-CN.md) 或 [CLI](run-with-cli.zh-CN.md) 中的任意一种),
  并且已经有数据在流入。

## 步骤

1. 前往 **Logs**、**Traces** 或 **Metrics** 中的任意一个页面,构建您想作为面板的
   查询——一个搜索词、一个服务过滤器、一个时间范围,或者(在 Metrics 页面上)一个
   具体选中的指标。
2. 点击该页面工具栏中的 **Pin to dashboard**。
3. 在 **Dashboard** 下拉菜单中选择一个已有的仪表板,或者保持选中 **New dashboard…**
   以当场创建一个新的(在那里命名)。
4. 为面板设置一个标题(已预填了合理的默认值——页面名称,或者在 Metrics 页面上是所选
   指标的名称),然后点击 **Pin**。
5. 从任意页面重复上述操作,可以固定到同一个或不同的仪表板,想要多少个面板都可以。

在顶部导航中打开 **Dashboards**,可以看到您创建的每一个仪表板;点击其中一个的
**Open**,即可查看其所有面板堆叠在一起,每个面板都显示实时数据。

## 管理仪表板

在 **Dashboards** 页面上,您可以:

- **创建**一个空白仪表板(然后按上述步骤从 Logs/Traces/Metrics 向其固定面板)——
  目前还无法直接从仪表板页面本身添加或配置面板。
- **重命名**仪表板的名称或描述。
- **删除**一个仪表板。这只会删除仪表板对象本身——绝不会影响底层的
  Logs/Traces/Metrics 数据。

在仪表板自己的页面上,每个面板都有一个**移除**操作(其标题栏中的垃圾桶图标),
只会将该面板从仪表板中移除。

## 验证

打开一个至少有一个面板的仪表板,确认每个面板都显示真实、最新的数据(而不是卡在
加载状态或为空)——Logs 面板显示其通常的事件量图表,Metrics 面板显示其通常的
折线/百分位图表,Traces 面板显示其通常的追踪列表,与它们各自页面上的样子完全一致。

## 已保存搜索与仪表板

两者都始于同一个起点——您在 Logs/Traces/Metrics 上构建的查询——但解决的是不同的
问题:

| | 已保存搜索(**Views**) | 仪表板 |
|---|---|---|
| 范围 | 单个页面的过滤器状态 | 多个面板,任意页面组合 |
| 重新打开时 | 替换当前页面的过滤器 | 并排显示所有面板 |
| 最适合 | “快速回到这个精确的搜索” | “一眼看到多项内容” |

对同一个查询,同时拥有已保存搜索和仪表板面板并不冲突——固定面板不会消耗或删除
已保存的搜索。

## 已知的局限,明确说明

这是仪表板功能的第一个版本,范围有意保持狭窄:

- **尚不支持拖拽调整大小或重新排序**——面板按固定时的顺序堆叠,每行一个,大小
  都相同。
- **没有仪表板级别的统一时间范围**——每个面板保留其被固定那一刻的时间范围(以及
  所有其他过滤条件),彼此独立。要更改某个面板的时间范围,需要重新固定它。
- **没有自动刷新**——需要重新加载页面才能看到更新的数据。
- **仪表板对所有已登录用户可见**,与目前的已保存搜索和告警规则一样——尚不支持
  按用户所有权或私有仪表板。

以上这些都不是永久性限制,只是尚未实现。
